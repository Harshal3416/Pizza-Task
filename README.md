# Pizza

This application is developed to place an order and view order status.

Run `npm i` and then `ng serve` to start this application.

## Data

Maintained a JSON file to store a set of static data.
Path: `src\assets\data.json`

## Storage

Used Localstorage to store order details, including status.

## Bootstrap

Used Bootstrap classes

## Images

Downloaded images from internet
